# Contributing

Please open an issue before changing the registered protocol. Code changes require tests, `make
check`, and `make verify`. Never edit a lock file or confirmation artifact in place. A protocol
amendment must be a new, dated file that states whether confirmation outcomes were visible.

Generated datasets, checkpoints, and caches are intentionally excluded from Git. Manifests,
selection lists, scalar results, figures, notebooks, and environment metadata are versioned.
